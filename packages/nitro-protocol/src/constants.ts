import {utils} from 'ethers';

export const DEFAULT_TOKEN_NAME = 'TestToken';
export const DEFAULT_TOKEN_SYMBOL = 'TEST';
export const DEFAULT_INITIAL_TOKEN_SUPPLY = utils.parseUnits('100000000', 18);
