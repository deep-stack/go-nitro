name: Publish npm package to gitea
on:
  release:
    types: [published]
jobs:
  npm_publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        # https://github.com/NomicFoundation/hardhat/issues/3877
      - name: Use Node.js 18.15
        uses: actions/setup-node@v3
        with:
          node-version: "18.15.0"
      - name: Run yarn build
        run: yarn && yarn build
      - name: Configure 3a54-14-140-185-65.ngrok-free.app npm registry
        run: |
          npm config set registry https://3a54-14-140-185-65.ngrok-free.app/api/packages/cerc-io/npm/
      - name: Authenticate to 3a54-14-140-185-65.ngrok-free.app registry
        run: |
          npm config set -- '//3a54-14-140-185-65.ngrok-free.app/api/packages/cerc-io/npm/:_authToken' "${{ secrets.GITEA_PUBLISH_TOKEN }}"
      - name: Publish nitro-protocol package
        run: |
          cd ./packages/nitro-protocol
          npm publish
      - name: Publish nitro-rpc-client package
        run: |
          cd ./packages/nitro-rpc-client
          npm publish
